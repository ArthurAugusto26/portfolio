function testa form (){

    var aluno =<document class="form">["formalunos] {"aluno"].value;</document>
    var CPF= <document class="form"></document>["formalunos"] ["cpf"].value;
      
    if (aluno==""){

        alert ("Nome do aluno precisa ser informado. ")
        document.forms["formalunos"] ["alunos"].focus ();
           
        return false;

    }
}

if (cpf=="") {

    alert ( " CPF do aluno precisa ser informado") ;

    document.forms [ "formalunos"] ["cpf"].focus();

    return false ;

}
return true;

